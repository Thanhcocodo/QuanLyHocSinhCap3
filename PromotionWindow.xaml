<Window x:Class="QuanLyHocSinh.PromotionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:QuanLyHocSinh"
        Title="Xử lý Lên lớp cuối năm" Height="600" Width="800"
        WindowStartupLocation="CenterOwner">
    <Window.Resources>
        <local:BoolToYesNoConverter x:Key="BoolToYesNo"/>
    </Window.Resources>
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Xử lý Lên lớp / Tốt nghiệp cuối năm học" Grid.Row="0"
                   FontSize="16" FontWeight="Bold" Foreground="#1a237e" Margin="0,0,0,12"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12">
            <TextBlock Text="Năm học:" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <ComboBox x:Name="NamHocCombo" Width="160" Height="32" Margin="0,0,12,0"/>
            <Button Content="🔍 Xem trước kết quả" Width="180" Height="32"
                    Background="#1565c0" Foreground="White" Click="Preview_Click"/>
            <TextBlock x:Name="SummaryText" VerticalAlignment="Center" Margin="16,0,0,0"
                       Foreground="#555"/>
        </StackPanel>

        <DataGrid x:Name="PreviewGrid" Grid.Row="2" AutoGenerateColumns="False"
                  IsReadOnly="True" CanUserAddRows="False" AlternatingRowBackground="#f5f5f5"
                  GridLinesVisibility="Horizontal">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Mã HS" Binding="{Binding MaHocSinh}" Width="90"/>
                <DataGridTextColumn Header="Họ tên" Binding="{Binding HoTen}" Width="180"/>
                <DataGridTextColumn Header="Lớp hiện tại" Binding="{Binding LopHienTai}" Width="100"/>
                <DataGridTextColumn Header="Khối" Binding="{Binding Khoi}" Width="60"/>
                <DataGridTextColumn Header="TB tổng" Binding="{Binding TBTong, StringFormat=N2}" Width="80"/>
                <DataGridTemplateColumn Header="Đủ điều kiện" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding DuDieuKien, Converter={StaticResource BoolToYesNo}}"
                                       HorizontalAlignment="Center"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Kết quả" Binding="{Binding KetQua}" Width="100">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Style.Triggers>
                                <Trigger Property="Text" Value="Tốt nghiệp">
                                    <Setter Property="Foreground" Value="#2e7d32"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                </Trigger>
                                <Trigger Property="Text" Value="Lên lớp">
                                    <Setter Property="Foreground" Value="#1565c0"/>
                                </Trigger>
                                <Trigger Property="Text" Value="Ở lại">
                                    <Setter Property="Foreground" Value="#c62828"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Ghi chú" Binding="{Binding GhiChu}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <TextBlock x:Name="StatusText" VerticalAlignment="Center" Margin="0,0,16,0" Foreground="#555"/>
            <Button Content="Đóng" Width="90" Margin="0,0,8,0" Click="Close_Click"/>
            <Button x:Name="ApplyBtn" Content="✅ Áp dụng lên lớp" Width="160" Height="34"
                    Background="#2e7d32" Foreground="White" FontWeight="Bold"
                    IsEnabled="False" Click="Apply_Click"/>
        </StackPanel>
    </Grid>

</Window>
